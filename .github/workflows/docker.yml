name: Docker build

on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build image
        run: docker build -t docker-exercise .
      - name: Run container
        run: docker run docker-exercise
